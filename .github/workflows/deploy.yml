name: Deploy to production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest code
        run: git -C ~/rez.ai pull

      - name: Rebuild and restart
        run: docker compose -f ~/rez.ai/docker-compose.yml up -d --build
